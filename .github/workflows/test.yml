name: test 

on:
  schedule:
    - cron: "04 19 * * *"
jobs:
  test:
    runs-on: [self-hosted]
    steps:

      - name: test
        uses: actions/checkout@v2
        with:
          ref: ${{ if (github.event_name == 'schedule') }}testSchedule${{ else }}master${{ endif }}
          fetch-depth: 0
      
      - name: test echo
        run: echo "done"
